'use strict'

/* eslint-env mocha */
'use strict'

const chai = require('chai')
const dirtyChai = require('dirty-chai')
const expect = chai.expect
chai.use(dirtyChai)

describe('analytics', () => {
  let node
  let analytics
  before(() => {
    
  })

  it('send request', () => {

  })
})
